"""hypelysis: read a document on its merits.

A study settles what a document's terms mean — as entries a checked run
accepts, defers, or escalates to the study owner — before the document's own
claims are examined. The owner's approval at each milestone is an input the
machinery waits for, not a step it performs.
"""
__version__ = "0.1.0"
